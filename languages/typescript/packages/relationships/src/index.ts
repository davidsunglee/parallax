/**
 * `@parallax/relationships` — M4 relationships & deep fetch.
 *
 * Navigation, the correlated-EXISTS semi-join, and per-level deep fetch land in
 * Phase 5 (temporal propagation in Phase 6). This phase ships the package shell
 * only.
 */
export {};
