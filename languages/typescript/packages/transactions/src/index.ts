/**
 * `@parallax/transactions` — M8 transactions, unit of work & caches.
 *
 * The closure-demarcated unit of work, buffered/combined writes, and read lock
 * land in Phase 7. This phase ships the package shell only.
 */
export {};
