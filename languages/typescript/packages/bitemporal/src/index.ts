/**
 * `@parallax/bitemporal` — M7 bitemporal / milestoning.
 *
 * As-of predicate injection and audit-only milestone-chaining writes land in
 * Phase 6. This phase ships the package shell only.
 */
export {};
