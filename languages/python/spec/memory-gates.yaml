# Memory gates: the blocking ceilings the cost class grades the structural
# read and write windows against. Every gate is derived from the retained
# capture named under `basis` by one rule — the largest reading of an
# address on any supported runtime, scaled by `headroom` and rounded up to a
# whole byte — and `tests/unit/conformance/test_budget_contract.py` recomputes
# every entry from that capture, so no ceiling here is a transcription. Python
# reads this file through `parallax.conformance.budget.MemoryGates`; timing is
# never gated and its allowances under `advisory` are what the report's
# comparisons read a delta against.
schemaVersion: 1
basis:
  portfolio: languages/python/docs/structural-metadata-envelope/recovered/portfolio.json
  headroom: 1.10
advisory:
  timingNoiseAllowance: 0.05
  memoryNoiseAllowance: 0.03
  timingNoiseFloor: 0.15
scaling:
  acquisition.columns:
    subject: write-lowering
    ordered: [acquisition.rows-8.columns, acquisition.rows-32.columns, acquisition.rows-128.columns]
    nonIncreasing: [retainedBytes, transientBytes]
  acquisition.document:
    subject: write-lowering
    ordered: [acquisition.rows-8.document, acquisition.rows-32.document, acquisition.rows-128.document]
    nonIncreasing: [retainedBytes, transientBytes]
gates:
  write-lowering:
    txtime.opening.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3080 }
      transientBytes: { unit: B/row, maxBytes: 17110 }
    txtime.changed.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 4242 }
      transientBytes: { unit: B/row, maxBytes: 16819 }
    txtime.unchanged.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 4297 }
      transientBytes: { unit: B/row, maxBytes: 16819 }
    plain.changed.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3424 }
      transientBytes: { unit: B/row, maxBytes: 17233 }
    bitemporal.interior.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 6389 }
      transientBytes: { unit: B/row, maxBytes: 17407 }
    txtime.opening.columns.wire:
      retainedBytes: { unit: B/row, maxBytes: 2871 }
      transientBytes: { unit: B/row, maxBytes: 16793 }
    txtime.changed.columns.wire:
      retainedBytes: { unit: B/row, maxBytes: 4068 }
      transientBytes: { unit: B/row, maxBytes: 16533 }
    txtime.unchanged.columns.wire:
      retainedBytes: { unit: B/row, maxBytes: 4068 }
      transientBytes: { unit: B/row, maxBytes: 16533 }
    plain.changed.columns.wire:
      retainedBytes: { unit: B/row, maxBytes: 3195 }
      transientBytes: { unit: B/row, maxBytes: 16947 }
    bitemporal.interior.columns.wire:
      retainedBytes: { unit: B/row, maxBytes: 6266 }
      transientBytes: { unit: B/row, maxBytes: 17398 }
    txtime.opening.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3135 }
      transientBytes: { unit: B/row, maxBytes: 17110 }
    txtime.changed.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 4297 }
      transientBytes: { unit: B/row, maxBytes: 16819 }
    txtime.unchanged.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 4187 }
      transientBytes: { unit: B/row, maxBytes: 16819 }
    plain.changed.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3424 }
      transientBytes: { unit: B/row, maxBytes: 17233 }
    bitemporal.interior.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 6279 }
      transientBytes: { unit: B/row, maxBytes: 16984 }
    txtime.opening.document.wire:
      retainedBytes: { unit: B/row, maxBytes: 2871 }
      transientBytes: { unit: B/row, maxBytes: 16793 }
    txtime.changed.document.wire:
      retainedBytes: { unit: B/row, maxBytes: 4068 }
      transientBytes: { unit: B/row, maxBytes: 16533 }
    txtime.unchanged.document.wire:
      retainedBytes: { unit: B/row, maxBytes: 4026 }
      transientBytes: { unit: B/row, maxBytes: 16533 }
    plain.changed.document.wire:
      retainedBytes: { unit: B/row, maxBytes: 3195 }
      transientBytes: { unit: B/row, maxBytes: 16947 }
    bitemporal.interior.document.wire:
      retainedBytes: { unit: B/row, maxBytes: 6266 }
      transientBytes: { unit: B/row, maxBytes: 16907 }
    geometry.depth-1.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 2836 }
      transientBytes: { unit: B/row, maxBytes: 16705 }
    geometry.depth-1.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 2781 }
      transientBytes: { unit: B/row, maxBytes: 16705 }
    geometry.depth-4.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3575 }
      transientBytes: { unit: B/row, maxBytes: 17444 }
    geometry.depth-4.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3520 }
      transientBytes: { unit: B/row, maxBytes: 17444 }
    geometry.depth-8.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 4561 }
      transientBytes: { unit: B/row, maxBytes: 18958 }
    geometry.depth-8.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 4561 }
      transientBytes: { unit: B/row, maxBytes: 18958 }
    geometry.many-0.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 2220 }
      transientBytes: { unit: B/row, maxBytes: 16142 }
    geometry.many-0.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 2218 }
      transientBytes: { unit: B/row, maxBytes: 16142 }
    geometry.many-8.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 4367 }
      transientBytes: { unit: B/row, maxBytes: 18236 }
    geometry.many-8.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 4367 }
      transientBytes: { unit: B/row, maxBytes: 18236 }
    geometry.many-32.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 10437 }
      transientBytes: { unit: B/row, maxBytes: 30530 }
    geometry.many-32.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 10437 }
      transientBytes: { unit: B/row, maxBytes: 30709 }
    geometry.width-16.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3705 }
      transientBytes: { unit: B/row, maxBytes: 17699 }
    geometry.width-16.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3699 }
      transientBytes: { unit: B/row, maxBytes: 17699 }
    geometry.width-64.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 7401 }
      transientBytes: { unit: B/row, maxBytes: 26208 }
    geometry.width-64.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 7456 }
      transientBytes: { unit: B/row, maxBytes: 27978 }
    geometry.sparse-64.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 2781 }
      transientBytes: { unit: B/row, maxBytes: 16705 }
    geometry.sparse-64.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 2781 }
      transientBytes: { unit: B/row, maxBytes: 16705 }
    ancestor.depth-1.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3941 }
      transientBytes: { unit: B/row, maxBytes: 16573 }
    ancestor.depth-1.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3941 }
      transientBytes: { unit: B/row, maxBytes: 16573 }
    ancestor.width-16.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 4920 }
      transientBytes: { unit: B/row, maxBytes: 17567 }
    ancestor.width-16.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 4920 }
      transientBytes: { unit: B/row, maxBytes: 17567 }
    ancestor.width-64.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 8726 }
      transientBytes: { unit: B/row, maxBytes: 28885 }
    ancestor.width-64.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 8671 }
      transientBytes: { unit: B/row, maxBytes: 27065 }
    ancestor.sparse-64.columns.typed:
      retainedBytes: { unit: B/row, maxBytes: 3996 }
      transientBytes: { unit: B/row, maxBytes: 16573 }
    ancestor.sparse-64.document.typed:
      retainedBytes: { unit: B/row, maxBytes: 3996 }
      transientBytes: { unit: B/row, maxBytes: 16573 }
    acquisition.rows-8.columns:
      retainedBytes: { unit: B/row, maxBytes: 2822 }
      transientBytes: { unit: B/row, maxBytes: 6332 }
    acquisition.rows-32.columns:
      retainedBytes: { unit: B/row, maxBytes: 1995 }
      transientBytes: { unit: B/row, maxBytes: 4382 }
    acquisition.rows-128.columns:
      retainedBytes: { unit: B/row, maxBytes: 1785 }
      transientBytes: { unit: B/row, maxBytes: 3840 }
    acquisition.rows-8.document:
      retainedBytes: { unit: B/row, maxBytes: 4207 }
      transientBytes: { unit: B/row, maxBytes: 8651 }
    acquisition.rows-32.document:
      retainedBytes: { unit: B/row, maxBytes: 3316 }
      transientBytes: { unit: B/row, maxBytes: 6760 }
    acquisition.rows-128.document:
      retainedBytes: { unit: B/row, maxBytes: 3094 }
      transientBytes: { unit: B/row, maxBytes: 6211 }
    model.prepared:
      retainedBytes: { unit: B, maxBytes: 469929 }
      transientBytes: { unit: B, maxBytes: 479847 }
    model.prepared.family:
      retainedBytes: { unit: B, maxBytes: 24843 }
      transientBytes: { unit: B, maxBytes: 29850 }
  snapshot-delivery:
    read-depth-1:
      columns.peakKiB: { unit: KiB, maxBytes: 113516 }
      columns.retainedKiB: { unit: KiB, maxBytes: 57570 }
      document.peakKiB: { unit: KiB, maxBytes: 117696 }
      document.retainedKiB: { unit: KiB, maxBytes: 57570 }
    read-depth-4:
      columns.peakKiB: { unit: KiB, maxBytes: 168779 }
      columns.retainedKiB: { unit: KiB, maxBytes: 99388 }
      document.peakKiB: { unit: KiB, maxBytes: 171384 }
      document.retainedKiB: { unit: KiB, maxBytes: 99388 }
    read-depth-8:
      columns.peakKiB: { unit: KiB, maxBytes: 253193 }
      columns.retainedKiB: { unit: KiB, maxBytes: 155144 }
      document.peakKiB: { unit: KiB, maxBytes: 256607 }
      document.retainedKiB: { unit: KiB, maxBytes: 155144 }
    read-many-0:
      columns.peakKiB: { unit: KiB, maxBytes: 72094 }
      columns.retainedKiB: { unit: KiB, maxBytes: 27439 }
      document.peakKiB: { unit: KiB, maxBytes: 78598 }
      document.retainedKiB: { unit: KiB, maxBytes: 27439 }
    read-many-8:
      columns.peakKiB: { unit: KiB, maxBytes: 225053 }
      columns.retainedKiB: { unit: KiB, maxBytes: 141205 }
      document.peakKiB: { unit: KiB, maxBytes: 228199 }
      document.retainedKiB: { unit: KiB, maxBytes: 141205 }
    read-many-32:
      columns.peakKiB: { unit: KiB, maxBytes: 711314 }
      columns.retainedKiB: { unit: KiB, maxBytes: 482504 }
      document.peakKiB: { unit: KiB, maxBytes: 715015 }
      document.retainedKiB: { unit: KiB, maxBytes: 482504 }
    read-width-16:
      columns.peakKiB: { unit: KiB, maxBytes: 252130 }
      columns.retainedKiB: { unit: KiB, maxBytes: 154300 }
      document.peakKiB: { unit: KiB, maxBytes: 256161 }
      document.retainedKiB: { unit: KiB, maxBytes: 154300 }
    read-width-64:
      columns.peakKiB: { unit: KiB, maxBytes: 863449 }
      columns.retainedKiB: { unit: KiB, maxBytes: 541218 }
      document.peakKiB: { unit: KiB, maxBytes: 867479 }
      document.retainedKiB: { unit: KiB, maxBytes: 541218 }
    read-sparse-64:
      columns.peakKiB: { unit: KiB, maxBytes: 146953 }
      columns.retainedKiB: { unit: KiB, maxBytes: 40780 }
      document.peakKiB: { unit: KiB, maxBytes: 151133 }
      document.retainedKiB: { unit: KiB, maxBytes: 40780 }
    plan-depth-1:
      columns.peakKiB: { unit: KiB, maxBytes: 27528 }
      columns.retainedKiB: { unit: KiB, maxBytes: 18930 }
      document.peakKiB: { unit: KiB, maxBytes: 27703 }
      document.retainedKiB: { unit: KiB, maxBytes: 19105 }
    plan-depth-8:
      columns.peakKiB: { unit: KiB, maxBytes: 27528 }
      columns.retainedKiB: { unit: KiB, maxBytes: 18930 }
      document.peakKiB: { unit: KiB, maxBytes: 27703 }
      document.retainedKiB: { unit: KiB, maxBytes: 19105 }
    plan-width-64:
      columns.peakKiB: { unit: KiB, maxBytes: 27529 }
      columns.retainedKiB: { unit: KiB, maxBytes: 18931 }
      document.peakKiB: { unit: KiB, maxBytes: 27704 }
      document.retainedKiB: { unit: KiB, maxBytes: 19106 }
    control-guarded-1:
      plan.cold.peakKiB: { unit: KiB, maxBytes: 49980 }
      plan.cold.retainedKiB: { unit: KiB, maxBytes: 36129 }
    control-guarded-2:
      plan.cold.peakKiB: { unit: KiB, maxBytes: 67412 }
      plan.cold.retainedKiB: { unit: KiB, maxBytes: 50771 }
    control-guarded-3:
      plan.cold.peakKiB: { unit: KiB, maxBytes: 83858 }
      plan.cold.retainedKiB: { unit: KiB, maxBytes: 64823 }
