"""``parallax.conformance.cli`` enforcement scope (m-conformance-adapter).

The ``parallax-conformance`` console script. Skeleton stood up in COR-3
Phase 1; the describe/compile/run wire surface lands in COR-3 Phase 2.
"""

from __future__ import annotations


def main() -> int:
    """Console-script entry point.

    Phase 1 skeleton: the argv -> adapter core -> JSON wire surface is not
    wired yet, so every invocation reports a CLI usage error (exit code 2)
    per the m-conformance-adapter contract.
    """
    return 2
