"""The Parallax Python test suites, as one package rooted beside the workspace.

Every module under here is imported by its dotted path from the workspace root,
so `tests/unit/` can mirror the source tree directory for directory and a helper
can sit beside the tests that use it.
"""
