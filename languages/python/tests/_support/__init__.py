"""Cross-surface support for the Parallax Python test suites.

Helpers, models, and probes that more than one semantic surface needs. A test
module imports from here rather than from a sibling test module, so its audience
is explicit and collection order is irrelevant.
"""
