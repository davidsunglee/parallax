# Core expressions define predicates, assignments, and sort keys

Parallax has a shared expression model for predicates, assignments, and sort keys. Language APIs may spell that model differently, but they should map back to the same operation algebra.
