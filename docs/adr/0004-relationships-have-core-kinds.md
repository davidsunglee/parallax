# Relationships have core kinds

The core model distinguishes to-one, to-many, dependent, and association relationships. These kinds drive navigation, predicate quantification, ownership, and mutation semantics across language implementations.
