# TypeScript eager loading is called includes

The TypeScript API names eager relationship loading `includes` rather than `deepFetch`. `includes` is more natural in contemporary TypeScript APIs, while still mapping to Parallax's deep-fetch behavior and round-trip guarantees.
