# Includes load relationship paths

Parallax uses `includes` as the shared concept for eager relationship loading. Include entries are relationship paths, and a longer path implies the intermediate paths needed to load it.
