# Validation errors accumulate structured issues

Validation errors accumulate multiple structured issues when practical. Each issue carries stable location and code information so tools can report all relevant input problems without parsing messages.
