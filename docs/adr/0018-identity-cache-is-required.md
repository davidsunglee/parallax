# Identity cache is required

Parallax requires an identity cache concept so the same database identity can resolve to the same logical managed object within a cache scope. A language implementation may defer the first production-quality implementation, but the capability remains part of the target model.
