# Snapshots are detached data

Domain snapshots are the core detached data surface for serialization and API boundaries. They are plain data shapes, not unmanaged managed objects or objects that retain Parallax runtime behavior.
