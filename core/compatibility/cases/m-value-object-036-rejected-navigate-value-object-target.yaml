# Negative (m-value-object contract 4 / m-navigate, resolved Q7): relationship
# navigation aimed at a value object is refused PRE-SQL.
model: models/customer.yaml
tags: [m-value-object, m-navigate, rejected, negative, slice-snapshot-1, slice-managed-1]
shape: rejected
when:
  objectQuery:
    target: parallax.compatibility.Customer
    predicate:
      navigate:
        rel: parallax.compatibility.Customer.address
then:
  rejectedRule: navigate-value-object-target
