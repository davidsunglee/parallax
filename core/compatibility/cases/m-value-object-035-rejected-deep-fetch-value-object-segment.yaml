# Negative (m-value-object contract 4 / m-deep-fetch, resolved Q7): an Include Path
# containing a value-object segment is refused PRE-SQL.
model: models/customer.yaml
tags: [m-value-object, m-object-query, m-deep-fetch, rejected, negative, materialization, slice-snapshot-1, slice-managed-1]
shape: rejected
when:
  objectQuery:
    target: parallax.compatibility.Customer
    predicate:
      all: {}
    includes:
      - segments:
          - rel: parallax.compatibility.Customer.address
then:
  rejectedRule: deep-fetch-value-object-segment
