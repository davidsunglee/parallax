# Scenario-step PROJECTION WIDENING over a value-object-bearing temporal entity
# (m-value-object + m-txtime-write + m-unit-work + m-sql "Read result form"): the
# internal materialized-predicate-write resolving read is ROW-FORM — it constructs
# no instance — yet it projects the owner's `address` Document slot, because the
# per-row observation it records is a COMPLETE Predecessor Row (m-unit-work "A
# Predecessor Row is the complete, immutable persisted state a Temporal Observation
# retains"). Postgres AND MariaDB.
model: models/supplier.yaml
tags: [m-value-object, m-txtime-write, m-opt-lock, m-sql, m-unit-work, temporal, audit-only, json, nested, materialize, predicate-write, scenario, slice-snapshot-1]
shape: scenario
compileEligibility:
  mode: run-only
  reason: query-result-dependent
  note: "predicate write materializes per-row from a resolving read - per-row binds (matched keys / observed in_z) depend on the query result"
when:
  uow:
    concurrency: optimistic
  scenario:
    - objectQuery:
        target: parallax.compatibility.Supplier
        predicate:
          eq:
            attr: parallax.compatibility.Supplier.name
            value: Baltic Traders
        temporal:
          transaction-time:
            asOf: latest
      roundTrips: 1
      note: managed find materializes the owner WITH its address Document slot — INSTANCE-FORM
      statements:
        - sql:
            postgres: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.out_z = ?
            mariadb: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.out_z = ?
          binds: [Baltic Traders, infinity]
      expectRows:
        - sup_id: 2
          name: Baltic Traders
          in_z: "2024-02-01T00:00:00.000000Z"
          out_z: infinity
          address:
            street: 5 Port Road
            city: Riga
            geo:
              country: LV
            phones: []
    - objectQuery:
        target: parallax.compatibility.Supplier
        predicate:
          eq:
            attr: parallax.compatibility.Supplier.name
            value: Nordic Foods
        temporal:
          transaction-time:
            asOf: latest
      roundTrips: 1
      note: the predicate-write resolving read is ROW-FORM (no instance constructed) and still projects the address Document slot — the Temporal Observation it records is a complete Predecessor Row
      statements:
        - sql:
            postgres: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.out_z = ?
            mariadb: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.out_z = ?
          binds: [Nordic Foods, infinity]
      referenceSql: select sup_id, name, in_z, out_z, address from supplier where name = 'Nordic Foods' and out_z > '9000-01-01'
      expectRows:
        - sup_id: 1
          name: Nordic Foods
          in_z: "2024-06-01T00:00:00.000000Z"
          out_z: infinity
          address:
            street: 2 New Avenue
            city: Bergen
            geo:
              country: "NO"
            phones:
              - { type: work, number: '555-0200' }
    - write:
        mutation: terminate
        target:
          entity: parallax.compatibility.Supplier
          predicate:
            eq: { attr: parallax.compatibility.Supplier.name, value: Nordic Foods }
        at: "2024-10-01T00:00:00.000000Z"
      roundTrips: 1
      note: close the resolved current row (out_z = at), gated on step 1's observed in_z; a Transaction-Time-only close copies no payload, so no document rides this write's read
      statements:
        - sql:
            postgres: update supplier set out_z = ? where sup_id = ? and out_z = ? and in_z = ?
            mariadb: update supplier set out_z = ? where sup_id = ? and out_z = ? and in_z = ?
          binds: ["2024-10-01T00:00:00.000000Z", 1, infinity, "2024-06-01T00:00:00.000000Z"]
    - objectQuery:
        target: parallax.compatibility.Supplier
        predicate:
          eq:
            attr: parallax.compatibility.Supplier.name
            value: Nordic Foods
        temporal:
          transaction-time:
            asOf: "2024-08-01T00:00:00.000000Z"
      roundTrips: 1
      note: >-
        verify (INSTANCE-FORM managed find, same form as step 0, address Document slot present) —
        asOf-pins the Transaction-Time dimension strictly INSIDE the now-closed window
        [2024-06-01, 2024-10-01), so the containment predicate resolves the
        superseded milestone rather than defaulting to the current row; the
        RETURNED row's own out_z column (expectRows, graded by exact scalar
        equality regardless of the selecting predicate) is what proves the
        terminate landed at exactly 2024-10-01. A NON-EMPTY read that also
        exercises the instance-form address Document projection with real data.
      statements:
        - sql:
            postgres: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.in_z <= ? and t0.out_z > ?
            mariadb: select t0.sup_id, t0.name, t0.in_z, t0.out_z, not t0.address is null, t0.address from supplier t0 where t0.name = ? and t0.in_z <= ? and t0.out_z > ?
          binds: [Nordic Foods, "2024-08-01T00:00:00.000000Z", "2024-08-01T00:00:00.000000Z"]
      referenceSql: "select sup_id, name, in_z, out_z, address from supplier where name = 'Nordic Foods' and in_z <= '2024-08-01T00:00:00+00:00' and out_z > '2024-08-01T00:00:00+00:00'"
      expectRows:
        - sup_id: 1
          name: Nordic Foods
          in_z: "2024-06-01T00:00:00.000000Z"
          out_z: "2024-10-01T00:00:00.000000Z"
          address:
            street: 2 New Avenue
            city: Bergen
            geo:
              country: "NO"
            phones:
              - { type: work, number: '555-0200' }
then:
  roundTrips: 4
