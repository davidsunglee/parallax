# Boundary case (m-auto-retry): `maxRetries: 0` DISABLES the loop — even a transient failure
# surfaces to the caller — `api-conformance` lane.
model: models/account.yaml
tags: [m-auto-retry, m-opt-lock, m-api-conformance, transient, slice-snapshot-1, slice-managed-1]
lane: api-conformance
shape: boundary
compileEligibility:
  mode: run-only
  reason: single-connection
  note: "single-connection concurrency/locking intent (given.fault, shape:boundary)"
given:
  fault: serialization-failure
when:
  uow:
    concurrency: locking
    maxRetries: 0
  boundary:
    - action: read
      note: observe the versioned row
    - action: update
      note: the write raises an injected serialization failure — with maxRetries 0 it surfaces
then:
  outcome: serialization-failure
  roundTrips: 2
