# Includes exact deduplication — repeating one literal path is the same include set
# as spelling it once, so the canonical document and plan retain one child level.
model: models/orders.yaml
tags: [m-deep-fetch, includes-dedup]
shape: read
when:
  targetEntity: parallax.compatibility.OrderItem
  operation:
    deepFetch:
      operand: { all: {} }
      paths:
        - { segments: [{ rel: parallax.compatibility.OrderItem.order }] }
  equivalentEncodings:
    - deepFetch:
        operand: { all: {} }
        paths:
          - { segments: [{ rel: parallax.compatibility.OrderItem.order }] }
          - { segments: [{ rel: parallax.compatibility.OrderItem.order }] }
then:
  statements:
    - sql:
        postgres: select t0.id, t0.order_id, t0.sku, t0.quantity, t0.shipped_on from order_item t0
    - sql:
        postgres: select t0.id, t0.name, t0.sku, t0.qty, t0.price, t0.active, t0.ordered_on from orders t0 where t0.id in (?, ?, ?)
      binds: [1, 2, 42]
  referenceSql: select id, order_id, sku, quantity, shipped_on from order_item
  graph:
    OrderItem:
      - id: 11
        order_id: 1
        sku: A-100
        quantity: 2
        shipped_on: null
        order: { id: 1, name: Ada, sku: A-100, qty: 5, price: 10.50, active: true, ordered_on: 2024-01-05 }
      - id: 12
        order_id: 1
        sku: B-200
        quantity: 1
        shipped_on: 2024-02-15
        order: { id: 1, name: Ada, sku: A-100, qty: 5, price: 10.50, active: true, ordered_on: 2024-01-05 }
      - id: 21
        order_id: 2
        sku: A-300
        quantity: 4
        shipped_on: 2024-02-20
        order: { id: 2, name: Linus, sku: B-200, qty: 10, price: 20.00, active: true, ordered_on: 2024-02-10 }
      - id: 421
        order_id: 42
        sku: A-999
        quantity: 3
        shipped_on: 2024-03-10
        order: { id: 42, name: Grace, sku: A-999, qty: 30, price: 99.99, active: true, ordered_on: 2024-06-30 }
      - id: 422
        order_id: 42
        sku: B-200
        quantity: 5
        shipped_on: 2024-02-05
        order: { id: 42, name: Grace, sku: A-999, qty: 30, price: 99.99, active: true, ordered_on: 2024-06-30 }
  roundTrips: 2
