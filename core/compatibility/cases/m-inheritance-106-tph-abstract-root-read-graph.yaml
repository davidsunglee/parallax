# Table-per-hierarchy ABSTRACT-ROOT read, INSTANCE-FORM sibling: the developer-surface
# per-variant node shape (m-inheritance, m-case-format, m-sql).
model: models/payment.yaml
tags: [m-inheritance, m-sql, table-per-hierarchy, root, familyVariant, graph, instance-form, slice-snapshot-1, slice-managed-1]
shape: read
when:
  objectQuery:
    target: parallax.compatibility.Payment
    predicate:
      all: {}
then:
  statements:
    - sql:
        postgres: select t0.id, t0.kind, t0.amount, t0.card_network, t0.tendered from payment t0
  graph:
    Payment:
      - { id: 1, amount: "100.00", cardNetwork: Visa, familyVariant: CardPayment }
      - { id: 2, amount: "50.00", cardNetwork: Amex, familyVariant: CardPayment }
      - { id: 3, amount: "20.00", tendered: "25.00", familyVariant: CashPayment }
  roundTrips: 1
