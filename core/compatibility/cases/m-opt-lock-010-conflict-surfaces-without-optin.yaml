# Boundary case (m-opt-lock): an optimistic-lock conflict SURFACES to the caller when the
# unit of work does NOT opt into conflict retry — `api-conformance` lane.
model: models/account.yaml
tags: [m-opt-lock, m-auto-retry, m-api-conformance, conflict, slice-snapshot-1, slice-managed-1]
lane: api-conformance
shape: boundary
compileEligibility:
  mode: run-only
  reason: single-connection
  note: "single-connection concurrency/locking intent (given.fault, shape:boundary)"
given:
  fault: optimistic-lock-conflict
when:
  uow:
    concurrency: optimistic
    retryOptimisticConflicts: false
  boundary:
    - action: read
      note: observe the versioned row (records the observed version)
    - action: update
      note: the gated UPDATE conflicts once — with the opt-in unset it is NOT retried
then:
  outcome: optimistic-lock-conflict
  roundTrips: 2
