# contains + wildcard escape — the literal search term itself contains a SQL
# wildcard (`%`), which MUST be escaped so it matches literally. `contains '50%'`
# becomes `like '%50\%%' escape '\'`: the inner `\%` is a literal percent, wrapped
# in `%...%` for the contains semantics. Only `C_50%` contains the literal `50%`.
#
# This is the escape fixture the outline calls for: a fat-fingered escape would
# silently turn `%` back into a wildcard and over-match, which the independent
# referenceSql (using Postgres `position`) would catch.
model: models/orders.yaml
tags: [m-op-algebra, contains, string, escape, slice-snapshot-1, slice-managed-1]
shape: read
when:
  targetEntity: Order
  operation:
    contains:
      attr: Order.sku
      value: 50%
then:
  statements:
    - sql:
        postgres: select t0.id, t0.name, t0.sku, t0.qty, t0.price, t0.active, t0.ordered_on from orders t0 where t0.sku like ? escape ?
      binds: ["%50\\%%", "\\"]
  referenceSql: select id, name, sku, qty, price, active, ordered_on from orders where position('50%' in sku) > 0
  rows:
    - { id: 5, name: Alan, sku: "C_50%", qty: 25, price: 50.75, active: false, ordered_on: 2024-05-25 }
  roundTrips: 1
