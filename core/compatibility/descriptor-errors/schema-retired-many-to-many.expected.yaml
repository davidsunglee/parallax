# Direct many-to-many is unconstructible: `cardinality` is the closed
# one-to-one / many-to-one / one-to-many vocabulary, and the association shape
# is modeled as an explicit association Entity with two direct relationships.
# The retired `many-to-many` value fails the closed enum as one violation at
# the cardinality value itself — the defining branch is otherwise well-formed,
# so the rejection names the retired spelling precisely.
phase: schema
code: descriptor-schema-invalid
violations:
  - path: [entities, 0, relationships, 0, cardinality]
    rule: enum
