# Fixtures for the error-cases model. Only Gauge is seeded -- two rows the
# deadlock / lockWaitTimeout cases lock from opposite directions. Widget and Tag
# start empty and self-seed via each case's golden (the first INSERT succeeds,
# the second collides).
Gauge:
  - { id: 1, v: 0 }
  - { id: 2, v: 0 }
