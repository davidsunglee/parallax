# Fixture rows for the Subscriber model (models/subscriber.yaml).
#
# One version-1 row the optimistic value-object write case (m-value-object-046)
# reads and updates: a versioned document UPDATE replaces the whole `address`
# document and advances `version` 1 -> 2, gated on the observed version 1.
Subscriber:
  - id: 1
    version: 1
    address:
      street: 1 Elm St
      city: Rome
      geo:
        country: IT
