# Logical rows shared by both Storage Layout transparency descriptors. This file
# deliberately authors declared member names rather than physical cells.
parallax.compatibility.LayoutTwinItem:
  - { id: 1, label: alpha, rank: 20 }
  - { id: 2, label: beta, rank: 10 }
